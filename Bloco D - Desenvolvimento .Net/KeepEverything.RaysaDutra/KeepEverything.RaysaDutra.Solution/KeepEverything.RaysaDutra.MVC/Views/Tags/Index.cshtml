@model IEnumerable<KeepEverything.RaysaDutra.Presentation.Models.TagViewModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NameTag)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NameTag)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdTag }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdTag }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdTag })
        </td>
    </tr>
}

</table>
